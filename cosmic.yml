configserver:
    name: Casrak cosmic config server
    status: Connected to the Casrak cosmic!

# Enable /management/logfile endpoint for all apps
logging:
    path: /tmp
    file: ${spring.application.name}.log
